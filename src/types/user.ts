//Permet d'exporter les types TS pour les entrées API pour les services

//TYPE création User
export type CreateUserInput = {
    firstName: string;
    lastName: string;
};

//TYPE update User
export type UpdateUserInput = {
    firstName: string;
    lastName: string;
};
